description 'Neo Java compiler'

dependencies {
    compile project(':utils'),
            project(':model'),
            project(':crypto'),
            "org.ow2.asm:asm:$asmVersion",
            "com.fasterxml.jackson.core:jackson-databind:$jacksonVersion"
}
