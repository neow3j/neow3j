description 'NEO Application Binary Interface (ABI) based on NEP-3'

dependencies {
    compile project(':utils'),
            project(':model'),
            "com.fasterxml.jackson.core:jackson-databind:$jacksonVersion"
}
